** ENCUENTRA REGISTROS DUPLICADOS EN PEDIDOS

* CABECERAS
SELECT HP_NRO, COUNT(*) AS COUNTER  FROM V_PED_HD GROUP BY HP_NRO ORDER BY COUNTER DESC

* LINEAS
SELECT LP_NRO, LP_PART, COUNT(*) AS COUNTER  FROM V_PED_LN GROUP BY LP_NRO,LP_PART ORDER BY COUNTER DESC
FUNCTION V_CURMOV
*****************
PARAMETER T_TBL, T_DIR
ACTUAL=SELECT()
SELECT (T_TBL)
* MOVES CURSOR ON A TABLE
DO CASE
  CASE T_DIR=">"
    TRECNO=RECNO()
    SKIP
    IF EOF()
      GO TRECNO
    ENDIF 
  CASE T_DIR="<"
    IF .NOT. BOF()
      SKIP -1
    ENDIF 
  CASE T_DIR="^"
    GO TOP
  CASE T_DIR="v"
    GO BOTTOM
ENDCASE
SELECT (ACTUAL)
RETURN .T.

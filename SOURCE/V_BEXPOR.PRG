PROCEDURE V_BEXPOR
******************
*GENERA UN ARCHIVO TXT DESDE BROWSE QUE PUEDE SER GUARDADO
PARAMETER T_FIELDS, T_SCOPE

T_TITLE="[ESC] PARA SALIR - [F12] PARA GUARDAR"
DEFINE WINDOW W_BEXPORT FROM 0,0 TO 24,80 ;
       DOUBLE TITLE T_TITLE NOCLOSE
T_ARCH="..\TMP\"+SYS(3)+".TXT"
T_RECN=RECNO()

KEY12=ON("KEY", "F12")
ON KEY LABEL F12 KEYBOARD '{ALT+F}'+'V'
*ON KEY LABEL F12 DO NOTEPAD

LIST OFF FIELDS &T_FIELDS FOR EVALUATE(T_SCOPE) TO (T_ARCH) NOCONSOLE

GO T_RECN
ACTIVATE WINDOW W_BEXPORT
MODI COMM (T_ARCH) WINDOW W_BEXPORT NOEDIT
ON KEY LABEL F12 &KEY12
RELEASE WINDOW W_BEXPORT
DELETE FILE (T_ARCH)
RETURN

PROCEDURE NOTEPAD
*****************
! START NOTEPAD &T_ARCH
RETURN